.posts-wrapper.row
  .post
    .post-head
      .name
        User Name Here
    .image.center-block
      = image_tag @post.image.url(:medium)
    %p.caption
      = @post.caption
    .text-center
      = link_to "Cancel", posts_path
      |
      = link_to "Edit Post", edit_post_path(@post)
